# Database Configuration
DB_NAME=wikidb
DB_USER=wikiuser
DB_PASS=changeme_use_a_strong_password

# JWT Configuration
JWT_SECRET=changeme_generate_with_openssl_rand_base64_32
JWT_EXPIRES=8h

# LDAP Configuration (set LDAP_ENABLED=true to activate)
LDAP_ENABLED=false
LDAP_URL=ldap://ldap:389
LDAP_BIND_DN=cn=admin,dc=wiki,dc=local
LDAP_BIND_PW=changeme_ldap_admin_password
LDAP_SEARCH_BASE=ou=users,dc=wiki,dc=local
LDAP_SEARCH_FILTER=(uid={{username}})
LDAP_GROUP_BASE=ou=groups,dc=wiki,dc=local
